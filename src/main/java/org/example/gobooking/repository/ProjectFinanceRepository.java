package org.example.gobooking.repository;

import org.example.gobooking.entity.projectFinance.ProjectFinance;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectFinanceRepository extends JpaRepository<ProjectFinance,Integer> {
}
